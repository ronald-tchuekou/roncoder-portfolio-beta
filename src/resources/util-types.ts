export type NavLink = {
  url: string;
  label: string;
};

export type MotionTag = "div" | "a" | "span" | "p" | "h1";

export type Service = {
  id: string;
  key: string;
  title: string;
  description: string;
};
